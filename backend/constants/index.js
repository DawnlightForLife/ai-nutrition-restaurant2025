const roles = require('./roles');
const merchantTypes = require('./merchantTypes');
const genderOptions = require('./genderOptions');
const subscriptionPlans = require('./subscriptionPlans');

module.exports = {
  roles,
  merchantTypes,
  genderOptions,
  subscriptionPlans
};